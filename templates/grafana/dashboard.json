{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "id": null,
  "uid": "{{DOMAIN}}-overview",
  "title": "{{Domain}} — Temporal Overview",
  "tags": ["{{DOMAIN}}", "temporal", "demo"],
  "schemaVersion": 39,
  "version": 1,
  "time": { "from": "now-30m", "to": "now" },
  "templating": { "list": [] },
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Workflow task schedule-to-start (p99) — {{DOMAIN}} workflow queue",
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "prometheus-kind" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "targets": [
        {
          "refId": "A",
          "expr": "histogram_quantile(0.99, sum by (le) (rate(temporal_workflow_task_schedule_to_start_latency_seconds_bucket{namespace=\"{{DOMAIN}}\", task_queue=\"{{DOMAIN}}-workflow-task-queue\"}[5m])))",
          "legendFormat": "p99 schedule-to-start"
        }
      ]
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "Activity task schedule-to-start (p99) — {{DOMAIN}} activity queue",
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "datasource": { "type": "prometheus", "uid": "prometheus-kind" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "targets": [
        {
          "refId": "A",
          "expr": "histogram_quantile(0.99, sum by (le) (rate(temporal_activity_schedule_to_start_latency_seconds_bucket{namespace=\"{{DOMAIN}}\", task_queue=\"{{DOMAIN}}-activity-task-queue\"}[5m])))",
          "legendFormat": "p99 schedule-to-start"
        }
      ]
    }
  ]
}
