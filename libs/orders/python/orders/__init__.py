"""Orders domain core (ADR-0022): workflows, activities, client ports, proto
contracts, db models, and shared ids/errors. Reusable, not deployable — composition
(settings, DI, entrypoints, lifecycle) lives in the apps; generic kit in libs/appkit."""
