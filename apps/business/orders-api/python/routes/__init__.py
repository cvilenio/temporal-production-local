"""Route modules for the Orders REST API, grouped by surface: public order
operations (orders), workflow/activity persistence callbacks (internal), and the
destructive demo reset (admin). main.py includes each router."""
